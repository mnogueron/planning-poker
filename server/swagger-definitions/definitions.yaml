definitions:
  Poll:
    required:
      - id
      - name
      - description
      - userId
    properties:
      id:
        type: string
      name:
        type: string
      description:
        type: string
      userId:
        type: string
  Vote:
    required:
      - id
      - pollId
      - userId
      - value
    properties:
      id:
        type: string
      pollId:
        type: string
      userId:
        type: string
      value:
        type: string
  User:
    required:
      - id
      - name
      - authToken
    properties:
      id:
        type: string
      name:
        type: string
      authToken:
        type: string

  PollForm:
    required:
      - name
      - description
      - userId
    properties:
      name:
        type: string
      description:
        type: string
      userId:
        type: string
  VoteForm:
    required:
      - userId
      - value
    properties:
      userId:
        type: string
      value:
        type: string
  UserForm:
    required:
      - name
    properties:
      name:
        type: string
